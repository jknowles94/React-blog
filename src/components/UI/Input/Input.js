import React from 'react';
import './Input.scss';

const Input = (props) => {
  let input = null;
  // switch(props.inputType){
  //   case('input') {

  //   }
  // }
  return (
    <div>
      {input}
    </div>
  );
}

export default Input;
